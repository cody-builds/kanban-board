/** @type {import('next').NextConfig} */
const nextConfig = {
  // Regular Next.js app configuration
  // Environment variables with NEXT_PUBLIC_ prefix are automatically available client-side
};

export default nextConfig;
